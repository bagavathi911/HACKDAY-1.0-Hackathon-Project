services:
  - type: web
    name: ai-lsb-stegano
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn project.app:app --bind 0.0.0.0:$PORT
    autoDeploy: true
